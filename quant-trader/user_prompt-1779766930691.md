Act as a Principal Software Architect and a Senior Quant Developer. I want to build a production-grade, automated algorithmic trading application. Please provide a comprehensive, step-by-step system design blueprint and a foundational Python implementation for this application based on the following structural specifications.

### 1. System Architecture & Tech Stack
- Recommend a highly resilient, low-latency architecture (e.g., event-driven, microservices, or a multi-threaded monolithic structure suitable for a retail quant).
- Specify the tech stack for:
  - Language & Runtime (e.g., Python, C++, or Go)
  - Data Storage (High-frequency historical data vs. transactional state tracking)
  - Message Broker / Event Queue (e.g., Redis Pub/Sub, Kafka, or ZeroMQ)

### 2. Core Functional Components
Break down the application into modular blocks and explain the design of each:
- **Data Ingestion Engine:** How it manages real-time WebSockets and handles historical backfilling/REST API data gracefully without blocking the main loop.
- **Strategy & Signal Engine:** The abstract class structure where mathematical algorithms (e.g., Mean Reversion, Trend Following, or Machine Learning models) process data to emit Buy/Sell/Hold signals.
- **Risk Management Engine:** A strict, non-negotiable hard-coded wrapper that validates all signals before they touch the broker. Include maximum drawdown limits, position sizing parameters, and mandatory stop-loss/take-profit calculation rules.
- **Order Execution Manager:** Handles the actual broker API payload, monitors order states (Pending, Filled, Partially Filled, Cancelled), and handles network dropouts or partial fills.

### 3. API Integrations & Infrastructure
- Outline a standard setup for integrating with institutional or major retail APIs (e.g., Alpaca, Interactive Brokers, Binance, or OANDA).
- Detail how to secure API keys/secrets locally or in the cloud.
- Explain the infrastructure strategy for running this 24/7 (e.g., AWS EC2, Docker containers, uptime monitoring, and instant failure alerts via Telegram/Discord webhooks).

### 4. Backtesting vs. Live Switch
- Design the application to easily toggle between a "Backtest Mode" (reading from a local CSV/DB historical stream) and "Live/Paper Trading Mode" (subscribing to live WebSockets) using the exact same strategy logic.

### 5. Boilerplate Code Implementation
Provide a production-ready, clean, object-oriented Python boilerplate script that reflects this architecture. Include:
- An abstract `Strategy` class.
- A basic `MovingAverageCrossover` implementation.
- A mock `ExecutionManager` and `RiskManager`.
- A centralized `TradingEngine` event loop coordinating the flow.

Ask me clarifying questions about my target asset class (Stocks, Crypto, FX), my latency requirements, and my preferred broker if you need to optimize this blueprint further. Take a deep breath and break this down step-by-step.